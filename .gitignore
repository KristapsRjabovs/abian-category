# SQLite scratch files. category.db itself is committed only until the
# Postgres import migration runs in production; after that it can be deleted.
category.db-shm
category.db-wal

# Generated build artifacts
public/
netlify/functions/_data.json

# Environment / OS
.DS_Store
.env
__pycache__/
*.pyc
*.pyo
node_modules/

# Batch-generation manifests (one file per submitted Anthropic batch)
.batches/
